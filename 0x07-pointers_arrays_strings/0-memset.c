#include "main.h"

/**
 * simple_print_buffer - prints s in hexa
 * @s: the address of memory to print
 * @n: the size of the memory to print
 *
 * Return: s[i].
 */

char *_memset(char *s, char b, unsigned int n)
{
unsgined int i;
i = 0;
while ( i < size)
{
if (i % 10)
{
_puycahar(" ")
}
if (!(i % 10) && i)
{
_putchar("\n");
}
_putchar(s[i]);
i++;
}
_putchar("\n");
}
