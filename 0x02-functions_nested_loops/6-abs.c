#include "main.h"
#include <stdio.h>

/** 
 * _abs - absolute value of integer
 * @n: any integer number
 * Return: Alwyes 0
*/
int _abs(int n)
{
if (n > 0 && n == 0)
{
_putchar(n);
}
else
{
_putchar(-n);
}
return (0);
}
