#include "main.h"

/**
 * swap.c - swaps two integer
 * @c: swap number
 *
 * Return: Alweys 0.
 */
void swap_int(int *a, int *b)
{
	int c;
	_putchar("a=%d, b=%d\n", a, b);
	a = c;
	a = b;
	c = b;
	_putchar("a=%d, b=%d\n", a, b);
return (0);
}
