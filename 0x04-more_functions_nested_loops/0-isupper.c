#include "main.h"
#include <stdio.h>

/**
 * _isupper - check if c is uppercase.
 *@c: a char A to Z
 *
 * Return: Always 0.
 */

int _isupper(int c);
{
char = c;
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
